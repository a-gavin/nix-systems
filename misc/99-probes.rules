# 1BitSquared Tigard (FT2232D/C/H Chip)
#
# Has two separate USB interfaces. The first (iface 0) is dedicated to UART,
# the other (iface 1) is for one of SPI/I2C/, depending on what is configured on the board.
# 
# NOTE: The leading 0 in 'ID_USB_INTERFACE_NUM' is required for matching
ENV{ID_VENDOR_ID}=="0403", ENV{ID_MODEL_ID}=="6010", ENV{ID_USB_INTERFACE_NUM}=="00", MODE="0666", ENV{ID_MM_DEVICE_IGNORE}="1", ENV{ID_MM_PORT_IGNORE}="1", SYMLINK+="ttyTigard_0"
ENV{ID_VENDOR_ID}=="0403", ENV{ID_MODEL_ID}=="6010", ENV{ID_USB_INTERFACE_NUM}=="01", MODE="0666", ENV{ID_MM_DEVICE_IGNORE}="1", ENV{ID_MM_PORT_IGNORE}="1", SYMLINK+="ttyTigard_1"

# Pine64 USB JTAG Adapter (FT232H Chip)
ENV{ID_VENDOR_ID}=="0403", ENV{ID_MODEL_ID}=="6014", MODE="0666", ENV{ID_MM_DEVICE_IGNORE}="1", ENV{ID_MM_PORT_IGNORE}="1", SYMLINK+="ttyPine64"

# ESP USB JTAG Serial
ENV{ID_VENDOR_ID}=="303a", ENV{ID_MODEL_ID}=="1001", MODE="0666", ENV{ID_MM_DEVICE_IGNORE}="1", ENV{ID_MM_PORT_IGNORE}="1"

# ESP32 USB Serial (USB to UART by Silicon Labs)
#ENV{ID_VENDOR_ID}=="10c4", ENV{ID_MODEL_ID}=="ea60", MODE="0666", ENV{ID_MM_DEVICE_IGNORE}="1", ENV{ID_MM_PORT_IGNORE}="1", SYMLINK+="ttyESP32C3"

# Raspberry Pi Pico Picoprobe
ENV{ID_VENDOR_ID}=="2e8a", ENV{ID_MODEL_ID}=="000c", MODE="0666", ENV{ID_MM_DEVICE_IGNORE}="1", ENV{ID_MM_PORT_IGNORE}="1", SYMLINK+="ttyPicoprobe"

# Raspberry Pi Picoprobe (old product ID)
ENV{ID_VENDOR_ID}=="2e8a", ENV{ID_MODEL_ID}=="0004", MODE="0666", SYMLINK+="ttyPicoprobe"

# Raspberry Pi Pico Rust Picoprobe
ENV{ID_VENDOR_ID}=="1209", ENV{ID_MODEL_ID}=="4853", MODE="0666", SYMLINK+="ttyPicoprobeRust"

# Raspberry Pi Pico DapperMime Probe
ENV{ID_VENDOR_ID}=="cafe", ENV{ID_MODEL_ID}=="4005", MODE="0666", SYMLINK+="ttyPicoprobeDapperMime"

# Raspberry Pi Pico Rust-Dap Probe
ENV{ID_VENDOR_ID}=="6666", ENV{ID_MODEL_ID}=="4444", MODE="0666", SYMLINK+="ttyPicoprobeRustDap"
